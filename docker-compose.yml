version: "3.8"

services:
  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/app:Z
      - data:/app/data:Z
    environment:
      - POLYGON_RPC_URL=https://polygon-rpc.com
    restart: unless-stopped

volumes:
  data:
